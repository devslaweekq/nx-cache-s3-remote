# Installed layout — image from Docker Hub, no local build.
services:
  nx-cache-s3:
    image: slaweekq/nx-cache-s3:latest
    restart: unless-stopped
    ports:
      - '${PORT:-55100}:${PORT:-55100}'
    env_file:
      - .env
